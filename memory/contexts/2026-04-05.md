# 2026-04-05 セッションブリーフィング

## サマリー
cmux スキル改善（send/send-key）と cogmem dashboard 起動手順のメモリ保存。

## 引き継ぎ
- 継続テーマ: なし
- 次のアクション: なし
- 注意事項: cmux の `send-keys` は存在しない（`send` + `send-key` を使う）

## 主要決定
- dashboard 起動手順はプロジェクト固有のためスキルではなくメモリに保存
- cmux panes-surfaces リファレンスに send/長時間プロセスパターンを追加
